// schema.graphql
type User {
  id: ID!
  username: String!
  email: String!
}

type Query {
  login(email: String!, password: String!): User
}

type Mutation {
  register(username: String!, email: String!, password: String!): User
}